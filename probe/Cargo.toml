[package]
name = "probe"
version = "0.1.0"
edition = "2021"

[dependencies]
schema = { path = "../schema" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ash = "0.38"

[features]
live_gpu_tests = []
